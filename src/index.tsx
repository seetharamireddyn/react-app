import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <div>
      <h1>Junk Store</h1>
    </div>
  </React.StrictMode>,
  document.getElementById('root')
);
